# Lubina 知识库

> 面向初学者的技术文档。每个概念解释"是什么"和"为什么这样写"，附代码示例。

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 目录

1. [项目总览](#1-项目总览)
2. [技术栈大白话](#2-技术栈大白话)
3. [文件结构与调用链路](#3-文件结构与调用链路)
4. [文件树系统](#4-文件树系统)
5. [工作区安全模型](#5-工作区安全模型)
6. [CodeMirror 6 编辑器](#6-codemirror-6-编辑器)
7. [Tauri v2 插件与 IPC](#7-tauri-v2-插件与-ipc)
8. [Rust 自定义命令](#8-rust-自定义命令)
9. [Markdown 预览与 HTML 渲染](#9-markdown-预览与-html-渲染)
10. [依赖管理](#10-依赖管理)
11. [设计系统](#11-设计系统)
12. [开发命令速查](#12-开发命令速查)

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 1. 项目总览

Lubina = Tauri 2.0 桌面壳 + HTML/CSS/JS 前端 + Python FastAPI 后端。

```
+-------------------------------------------+
|           Tauri（Rust 写的壳）              |
|  +--------------------------------------+  |
|  |   WebView（Edge 浏览器内核）           |  |
|  |  +--------------------------------+  |  |
|  |  |   HTML/CSS/JS 前端              |  |  |
|  |  +--------------------------------+  |  |
|  +--------------------------------------+  |
|                                           |
|   Python 后端（Tauri 自动启动/关闭）       |
+-------------------------------------------+
```

| 术语 | 解释 |
|------|------|
| WebView | 桌面应用里的"迷你浏览器"，显示网页 |
| Tauri | 用 Rust 创建窗口，里面跑 WebView |
| Vite | 前端开发服务器，修改代码后自动刷新 |
| SPA | Single Page Application，所有页面在一个 HTML 里，JS 切换 |
| IIFE | 立即执行函数 `(function(){...})()`——JS 模块化写法 |

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 2. 技术栈大白话

### 前端：HTML + CSS + JS（纯原生，无框架）

```
index.html          ← 唯一的 HTML 文件，所有结构都在这里
js/
  app.js            ← 主控：页面路由、全局状态、快捷键（无分屏）
  chat.js           ← AI 对话：消息渲染、Markdown、流式 SSE
  editor.js         ← 代码编辑器：CM6、Ctrl+C/S、Markdown/HTML 预览
  file-explorer.js  ← 文件树：真实目录、懒加载、工作区根目录
  settings.js       ← 设置页：主题/字体/供应商/关于
  bridge.js         ← Tauri 桥接：对话框/文件系统/窗口控制/外部链接
  api.js            ← 后端 API：fetch 封装、流式聊天
styles/
  theme.css         ← 设计系统：CSS 变量（亮/暗）、按钮、动画
  layout.css        ← 布局：四区 flex、模态框
  components.css    ← 组件：消息气泡、文件树节点、编辑器、预览样式
```

### 后端：Python FastAPI

Tauri 启动时自动 spawn `python -m uvicorn backend.main:app`，退出时 kill。

### Rust 层：Tauri 插件 + 自定义命令

- `tauri-plugin-dialog`：原生文件夹选择对话框
- `tauri-plugin-fs`：文件系统（scope 限制太大，改用自定义命令）
- 自定义命令 `list_dir` / `read_file_content`：用 `std::fs` 直接读写

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 3. 文件结构与调用链路

### 3.1 JS 文件加载顺序（不能乱）

```
api.js → bridge.js → file-explorer.js → editor.js → chat.js → settings.js → app.js
```

`app.js` 中用到了前面所有模块（Chat/Editor/Settings/FileExplorer/Bridge），所以必须最后加载。

### 3.2 页面切换流程（含持久化）

```
用户点击活动栏按钮
  → App.showPage('editor')
    → unmountPage(旧页面)    // 如 Editor.destroy()
    → renderSingle('editor') // 写入 HTML
    → mountPage('editor')    // 如 Editor.mount()
```

### 3.3 文件打开流程（主面板）

```
用户点击文件树中的文件
  → FileExplorer.openFile(node)
    → App.showPage('editor')
    → Editor.openFile(node.path, node.name)
      → Bridge.readTextFile(path)       // 调用 Rust 命令
      → _createCMView()                 // 创建 CodeMirror 6 实例
        → import('codemirror')           // Vite 从 node_modules 解析
        → new EditorView({ doc, extensions, parent })
```

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 4. 文件树系统

### 4.1 文件夹选择

用 Tauri 对话框插件打开原生文件夹选择器：

```js
// bridge.js
async function openFolderDialog() {
  return await _invoke('plugin:dialog|open', {
    options: { directory: true, multiple: false, title: '选择文件夹作为工作区' }
  });
}
```

**关键点**：参数必须包一层 `{ options: {...} }`，不能直接传 `{ directory: true }`。这是 Tauri v2 dialog 插件的要求。

### 4.2 真实目录读取

```rust
// lib.rs — 自定义 Rust 命令，绕过 tauri-plugin-fs 的 scope 限制
#[tauri::command]
fn list_dir(path: String) -> Result<Vec<DirEntry>, String> {
    let dir = Path::new(&path);
    let entries = fs::read_dir(dir)?;
    // 收集、排序（目录优先 + 字母序）
    result.sort_by(|a, b| b.is_dir.cmp(&a.is_dir)
        .then(a.name.to_lowercase().cmp(&b.name.to_lowercase())));
    Ok(result)
}
```

**为什么不用 tauri-plugin-fs 的 readDir**：它有 scope 限制，默认只让读应用自身目录。用户选的桌面文件夹会报 `forbidden path`。

### 4.3 懒加载

大型目录不一次性全读——目录节点标 `lazy: true`，点击展开时才 `listDir`：

```js
if (isExpanding && node.lazy) {
  const entries = await Bridge.listDir(node.path);
  node.children = entries.map(...);
  node.lazy = false;
}
```

**为什么**：如果用户选了 `C:\`，一次性递归读取所有子目录会非常慢（数万个文件）甚至卡死。懒加载只读当前层，展开时才读下一层。

### 4.4 全局工作区根目录

```js
// file-explorer.js — 打开文件夹后
window.__lubina_workspace_root = rootPath;
```

挂到 `window` 上而不是在模块内部，是为了让 chat.js、app.js 等其他模块也能直接读到，不需要通过 FileExplorer 中转。

### 4.5 snake_case 陷阱

Rust 返回的 JSON 字段是 `is_dir`（snake_case），不是 JS 习惯的 `isDir`（camelCase）。bridge.js 的 `listDir()` 做了转换：

```js
e => ({ name: e.name, path: e.path, isDir: e.is_dir })
```

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

### 4.6 文件右键菜单



文件树中的文件节点支持右键菜单，根据文件类型动态显示不同选项：



| 文件类型 | 菜单项 |

|----------|--------|

| 所有文本文件 | 打开文件、在侧边打开 |

| .html / .htm | 打开文件、在侧边打开、在浏览器中打开 |

| .md | 打开文件、在侧边打开、以预览打开 |



- "打开文件" → 在编辑器的当前活跃面板打开

- "在侧边打开" → 在编辑器的另一个面板打开（自动激活分屏）

- "在浏览器中打开" → 调用 `Bridge.openExternal(filePath)` 用系统默认浏览器打开

- "以预览打开" → 打开文件后自动切换到预览模式



右键菜单使用 `.context-menu.file-menu` 类，点击外部或按 ESC 关闭。

## 5. 工作区安全模型

### 5.1 为什么需要

Agent（Plan/Auto 模式）会操作文件——读、写、创建、删除。如果没有任何限制，恶意 prompt 可能让 Agent 读到 `C:\Users\xxx\AppData\` 下的敏感文件。

### 5.2 两层防护

**前端（chat.js）**——发送前校验：
```js
if (chatMode === 'plan' || chatMode === 'agent') {
  if (!window.__lubina_workspace_root) {
    _showWorkspaceRequiredModal(); // 弹窗引导选文件夹
    return;
  }
}
```

**后端（将来）**——路径安全函数（app.js 已提供）：
```js
function isPathInWorkspace(filePath) {
  const root = window.__lubina_workspace_root;
  if (!root) return false;
  // 统一分隔符 + 小写比较（Windows 路径不区分大小写）
  const n = filePath.replace(/\\/g, '/').toLowerCase();
  const r = root.replace(/\\/g, '/').toLowerCase();
  // 禁止路径穿越攻击
  if (n.includes('..')) return false;
  return n.startsWith(r);
}
```

`..` 是路径穿越的经典手段（`/workspace/../secret.txt` → 逃逸到 `/secret.txt`），直接禁止最简单安全。

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 6. CodeMirror 6 编辑器

### 6.1 为什么用 CM6

| 问题 | textarea | CM6 |
|------|----------|-----|
| 行号对齐 | 手动 div + scroll 同步，滚动时易错位 | 共享 scrollDOM，永不偏移 |
| 自动换行行号 | 估算字符宽度计算视觉行，不准 | 原生支持，精准 |
| 语法高亮 | 需手写正则（没做） | 按扩展名加载专业语言包 |
| 大文件 | >100KB 卡 | Viewport 只渲染可见行，流畅 |

### 6.2 安装方式：npm（不是 CDN）

```bash
npm install codemirror @codemirror/lang-javascript @codemirror/lang-python ...
```

**为什么不用 CDN**：尝试了 5 种 CDN 方案全部失败：
- esm.sh 的 `codemirror` 和 `@codemirror/view` 是分开发送的，内部版本不一致 → `undefined extension` 错误
- Facet 类型的 `.of()` 方法在某些导入方式下丢失 → `Facet.of is not a function`
- Vite 拦截裸标识符 `import('codemirror')` 尝试本地解析
- importmap 方案与 Vite 冲突

npm 安装后 Vite 从本地 node_modules 解析，lock 文件保证所有子包版本一致，零问题。

### 6.3 核心创建代码

```js
import { EditorView, basicSetup, keymap } from 'codemirror';

const extensions = [
  // basicSetup 含：行号、高亮活动行、特殊字符、选中绘制、历史、缩进、括号匹配、搜索快捷键
  ...[].concat(basicSetup),

  // Ctrl+S 保存
  keymap.of([{ key: 'Mod-s', run: saveFile, preventDefault: true }]),

  // Ctrl+C 复制（显式绑定，弥补 Tauri webview 原生复制不稳定）
  keymap.of([{ key: 'Mod-c', run: copyText, preventDefault: true }]),

  // 文件修改标记（带初始化保护）
  EditorView.updateListener.of(u => {
    if (u.docChanged && _cmSettled) {  // _cmSettled 防止初始化误触发
      file.modified = true;
    }
  }),
];

const view = new EditorView({ doc: 'Hello', extensions, parent: domEl });
```

### 6.4 初始化保护（_cmSettled 计时器）

**问题**：CM6 创建编辑器后，语言扩展、缩进规范化等会触发多次 `docChanged` 事件。如果用单一的 `initial = true` 标志只跳第一次，剩余的仍会误标记文件为"已修改"。

**解决方案**：用 300ms `setTimeout` 设置 `_cmSettled` 标志，这个窗口期内所有 `docChanged` 一律忽略：

```js
let _cmSettled = false;
setTimeout(() => { _cmSettled = true; }, 300);
// updateListener 中：if (u.docChanged && _cmSettled) { ... }
```

**为什么 300ms**：CM6 初始化通常 50-150ms 内完成，300ms 留了足够余量。即使慢速设备也不会误判。

### 6.5 Ctrl+C 显式绑定

**问题**：Ctrl+A/Z/X/V 有效但 Ctrl+C 不生效。CM6 的 `basicSetup` 自带 `standardKeymap`（包含 `Mod-c`），但 Tauri 2 webview 中原生复制事件可能不稳定。

**解决方案**：显式添加 `Mod-c` 键绑定，用 Clipboard API + execCommand 兜底：

```js
keymap.of([{
  key: 'Mod-c',
  run: (view) => {
    const text = view.state.sliceDoc(view.state.selection.main.from, view.state.selection.main.to);
    if (text) {
      navigator.clipboard.writeText(text).catch(() => {
        // 兜底：textarea + execCommand('copy')
        const ta = document.createElement('textarea');
        ta.value = text; ta.style.position = 'fixed'; ta.style.left = '-9999px';
        document.body.appendChild(ta); ta.select();
        document.execCommand('copy');
        document.body.removeChild(ta);
      });
    }
    return true;  // 阻止默认，由我们处理
  }
}])
```

### 6.6 Compartment 是什么（历史参考）

Compartment 是 CM6 官方的"可替换扩展槽位"，曾用于自动换行切换。自动换行功能已删除，相关代码不存于当前版本。保留此说明供将来参考。

### 6.7 语法高亮

```js
const LANG = {
  js:  async () => { const m = await import('@codemirror/lang-javascript'); return m.javascript(); },
  py:  async () => { const m = await import('@codemirror/lang-python'); return m.python(); },
  md:  async () => { const m = await import('@codemirror/lang-markdown'); return m.markdown(); },
  // ...
};
```

每个语言包的 `import()` 是动态的——只有打开对应类型文件时才加载，不浪费带宽。加载过一次后缓存到 `_langCache`。

### 6.8 主题（深色·浅色双模式）

编辑器主题分成两层，走不同的 API：

| 层 | API | 管什么 |
|----|-----|--------|
| 外壳 | `EditorView.theme()` | 背景色、行号区、光标颜色、选中高亮、字体大小和粗细 |
| 语法高亮 | `HighlightStyle.define()` + `syntaxHighlighting()` | 关键词、字符串、注释、变量名等的颜色 |

**为什么语法高亮不能放在 `EditorView.theme()` 里**：CM6 的语法着色不走 CSS 类名（DOM 上没有 `.cm-keyword` 这种东西），而是通过 tag→style 映射系统。`HighlightStyle.define()` 将语义 tag（`tags.keyword`、`tags.string` 等）映射为颜色，`syntaxHighlighting()` 将其注入编辑器。如果写在 `EditorView.theme()` 的 `.cm-keyword` 选择器里，完全匹配不到 DOM，等于没写。

**深色模式语法配色原则**：所有色值的 HSL 亮度 ≥ 70%，偏白高亮，拒绝暗色调。

| tag | 颜色 | 语法元素 |
|-----|------|---------|
| `tags.keyword` | `#c8c8e0` | if / for / return |
| `tags.string` | `#c8d4c0` | "hello" |
| `tags.comment` | `#6e6e7a` | // 注释（唯一暗色） |
| `tags.variableName` | `#d4d2d8` | 变量名 |
| `tags.tagName` | `#c0c8d8` | `<div>` |
| `tags.heading` | `#d4d0c8` + bold | # 标题 |

**字体粗细**：深色模式下 `fontWeight: '450'`（比默认 400 稍粗），行号保持 400 不加粗。

**需要额外导入的包**（从 node_modules，已是 codemirror 的依赖）：
```js
import('@codemirror/language')   // → syntaxHighlighting, HighlightStyle
import('@lezer/highlight')       // → tags
```

### 6.9 双面板架构（v5）



编辑器 v5 支持左右分屏，同时编辑/预览两个文件。



**数据结构**：

```js

let panels = [

  { openFiles: [], activeFileIndex: -1, cmView: null, previewMode: false },

  { openFiles: [], activeFileIndex: -1, cmView: null, previewMode: false }

];

let activePanel = 0;      // 当前聚焦的面板（0=左 1=右）

let splitActive = false;  // 右侧面板是否可见

```



**DOM 元素命名约定**：每个面板的 DOM 元素以数字后缀区分。辅助函数 `_el(suffix, panelIdx)` 返回如 `editorTabs0`、`editorCmWrapper1` 等元素。



**分屏核心函数**：

- `toggleSplit()` — 显示/隐藏右侧面板，绑定拖拽手柄

- `openFileInSide(filePath, fileName)` — 在非活跃面板打开文件，自动激活分屏

- `focusPanel(pIdx)` — 切换活跃面板，渲染两个面板的标签样式



**持久化**：`destroy()` 时保存 `panels` 和 `splitActive` 到 `_savedState`，`mount()` 时完整恢复（CM6 实例随 DOM 重新创建）。修改标记在恢复后清除。



**标签栏操作区**：`_renderTabs()` 在标签栏右侧渲染 `.editor-tabs-actions` 区域，包含预览切换按钮（仅 MD/HTML 文件显示）和分屏切换按钮。不再使用独立的 `editor-preview-toolbar` 行。

```js
EditorView.theme({
  '&': {
    backgroundColor: isDark ? '#1a1a24' : '#fafafc',
    color: isDark ? '#e6e4f0' : '#3a3650',  // 深色下 #e6e4f0 比旧 #d4d2e0 更亮更易读
    fontSize: '0.92rem',                     // 比默认 0.85rem 大，小白友好
    fontFamily: '"Cascadia Code","Fira Code",...',
  },
  '.cm-gutters': { ... },    // 行号区
  '.cm-activeLine': { ... }, // 当前行高亮
  '.cm-cursor': { ... },     // 光标
}, { dark: isDark });
```

`EditorView.theme()` 生成一个扩展，把 CSS 注入到 CM6 的 DOM。配色值严格从 `theme.css` 中提取。

### 6.9 降级保护

CDN / 网络出问题时 CM6 可能加载失败。editor.js 有完整的 fallback：

```js
try {
  cmView = new EditorView({ ... });
} catch (e) {
  // 回退到 textarea + 手动行号
  parent.innerHTML = `<div class="editor-body">
    <div class="editor-gutter"></div>
    <textarea class="editor-textarea">${content}</textarea>
  </div>`;
}
```

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 7. Tauri v2 插件与 IPC

### 7.1 window.__TAURI__ vs __TAURI_INTERNALS__

| 对象 | 哪来的 | 本项目中能用吗 |
|------|--------|---------------|
| `window.__TAURI__` | `@tauri-apps/api` npm 包 | **不能**（没装这个包） |
| `window.__TAURI_INTERNALS__` | Tauri 运行时自动注入 | **能用** |

本项目用 `__TAURI_INTERNALS__.invoke(cmd, args)` 调用所有 Tauri 能力。

### 7.2 插件注册必须三处联动

1. **Cargo.toml**：`tauri-plugin-dialog = "2"`
2. **capabilities/default.json**：`"dialog:allow-open"`
3. **lib.rs**：`.plugin(tauri_plugin_dialog::init())`

而且 `.plugin()` 必须在 Builder 链上调用，不能在 `setup()` 闭包里调用（太晚了，WebView 已初始化）。

### 7.3 打开外部链接（默认浏览器）

```js
// bridge.js
async function openExternal(url) {
  return _safeCall(async () => {
    await _invoke('plugin:shell|open', { path: url });
  }, (() => { window.open(url, '_blank'); })());
}
```

**使用场景**：设置→关于中的 CodeMirror 6 链接，点击时先弹权限确认框，用户同意后调用此函数在默认浏览器中打开。

**为什么不用 `window.open`**：Tauri WebView 中 `window.open` 不保证打开系统默认浏览器，可能在新 WebView 窗口中打开。`plugin:shell|open` 明确使用操作系统默认程序。

**前置条件**：`capabilities/default.json` 必须包含 `"shell:allow-open"` 权限，否则 IPC 调用直接静默失败。这是 Tauri v2 的安全模型——每个插件能力都需要显式声明。

### 7.4 参数格式因插件而异

```js
// dialog 插件 → 需要 { options: {...} } 包装
_invoke('plugin:dialog|open', { options: { directory: true } });

// 自定义 Rust 命令 → 参数直接传
_invoke('list_dir', { path: 'D:/something' });
```

没有统一标准，取决于 Rust 侧的函数签名。

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 8. Rust 自定义命令

### 8.1 为什么需要

`tauri-plugin-fs` 有 scope 安全限制，用户自选的目录（如桌面文件夹）会报 `forbidden path`。写自定义 `#[tauri::command]` 直接用 `std::fs`，完全绕过 scope。

### 8.2 实现

```rust
#[derive(serde::Serialize)]
struct DirEntry { name: String, path: String, is_dir: bool }

#[tauri::command]
fn list_dir(path: String) -> Result<Vec<DirEntry>, String> {
    fs::read_dir(Path::new(&path))
        .map_err(|e| format!("读取目录失败: {}", e))?
        .map(|entry| { ... })
        .collect()
}

#[tauri::command]
fn read_file_content(path: String) -> Result<String, String> {
    fs::read_to_string(&path).map_err(|e| format!("读取文件失败: {}", e))
}
```

### 8.3 注册

```rust
tauri::Builder::default()
    .invoke_handler(tauri::generate_handler![list_dir, read_file_content])
```

`generate_handler!` 是宏——它会为每个命令生成 IPC 路由代码。

### 8.4 前端调用

```js
// 自定义命令没有 plugin: 前缀，直接传命令名
const entries = await window.__TAURI_INTERNALS__.invoke('list_dir', { path: dirPath });
const content = await window.__TAURI_INTERNALS__.invoke('read_file_content', { path: filePath });
```

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 9. Markdown 预览与 HTML 渲染

### 9.1 为什么需要

用户打开 `.md` 文件时只看源码不够直观，想看渲染效果。`.html` 文件同样需要预览渲染结果。但我们不是要做 IDE —— 工具条简洁，一键切换。

### 9.2 实现方式

```
打开 .md 或 .html 文件
  → 编辑器 tab 栏右侧出现"预览"按钮（editor-tabs-actions）
  → 右键菜单也出现"预览/返回编辑"
  → 点击切换预览模式
```

**Markdown（.md）**：用 `marked` 库（npm 已安装）将 Markdown 源码转为 HTML，显示在 `<div class="markdown-body">` 中。动态 import，只有第一次打开 `.md` 文件时才加载。

```js
const marked = await import('marked');
const html = marked.parse(file.content);  // 或 marked.marked()
container.innerHTML = `<div class="markdown-body">${html}</div>`;
```

**HTML（.html）**：创建 `<iframe>` 并设置 `srcdoc` 属性为 HTML 源码。`sandbox="allow-scripts allow-same-origin"` 允许脚本但限制敏感行为。

```js
const iframe = document.createElement('iframe');
iframe.sandbox = 'allow-scripts allow-same-origin';
iframe.srcdoc = file.content;
```

### 9.3 预览模式生命周期

- `previewMode` 是模块级状态（`let previewMode = false`）
- 切换文件时自动退出预览模式（`switchFile` / `openFile` 都重置为 `false`）
- 销毁编辑器时清理预览 DOM

### 9.4 Markdown 样式

`components.css` 中的 `.markdown-body` 定义了完整的渲染样式：标题分级（h1-h6 不同大小）、代码块（等宽字体 + 背景色）、引用（左侧紫色边框）、表格（边框 + 斑马纹表头）、链接（主题色）。所有颜色适配深色/浅色两套。

### 9.5 为什么不用 Compartment 了

最初发布后，自动换行用 `Compartment` 动态切换 `EditorView.lineWrapping`。后来功能被整个删除（用户不想要），相关代码已从 editor.js / settings.js / components.css 完全移除。Compartment 是什么参见之前的版本记录（已不适用当前代码）。

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 10. 依赖管理

### 10.1 CDN → npm

marked 和 katex 原本从 jsdelivr CDN 加载，Tauri WebView 有 Tracking Prevention 警告。改为 npm 安装：

```html
<!-- 之前（CDN） -->
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

<!-- 现在（npm） -->
<script src="../node_modules/marked/marked.min.js"></script>
```

Vite 需要配置 `server.fs.allow: ["..", "../node_modules"]` 才能访问 src/ 外面的 node_modules。

### 10.2 生产构建注意事项

`npm run tauri build` 时 Tauri 直接读 `src/` 目录（不走 Vite），所以 HTML 中的 `../node_modules/` 路径在打包时需要特殊处理。更干净的方案是构建时把 marked/katex 复制到 src/ 内，或者用 Vite build 打包进 dist。

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 11. 设计系统

### 「深空月夜」配色（暗色模式）

| 角色 | 颜色 | 用途 |
|------|------|------|
| 主色 primary | `#5058A8` → `#727BC9` 渐变 | 按钮、活动指示、链接 |
| 辅助 secondary | `#B8B2D8` | 弱化元素 |
| 点缀 accent | `#8C2E50` 深酒红 | 危险操作、强调 |
| 深色背景 | `#1A1626` → `#1A1E3A` | 暗色模式 |
| 编辑器文字 | `#e6e4f0` | 暗色编辑器正文（提亮后更易读） |
| 编辑器行号 | `#7a75a0` | 暗色编辑器行号 |

### 「浅梦微光」配色（亮色模式）

浅色模式命名为"浅梦微光"—— UI 设计偏浅梦质感，与暗色"深空月夜"遥相呼应。浅梦对深空，微光对月夜，皆四字对仗。

| 角色 | 颜色 | 用途 |
|------|------|------|
| 亮色背景 | `#fafafc` | 亮色模式 |
| 卡片 | `#f3f1fb` 渐变 | 消息气泡、面板 |
| 编辑器文字 | `#3a3650` | 亮色编辑器正文 |

所有 hex 值以 `前端UI颜色设计参考.html` 为准。

### CM6 编辑器主题

编辑器配色从以上设计系统提取，通过 `EditorView.theme()` 注入。行号区、活动行、光标颜色都匹配亮/暗两套。

### 可缩放布局中的 CSS 单位选择：px vs em vs rem

**什么是 rem**：`rem` = "root em"，相对于 `<html>` 根元素的 `font-size`。本项目通过 `document.documentElement.style.fontSize = (13.5 * scale) + 'px'` 实现字体缩放，所有使用 `rem` 的属性都会随之缩放。

**什么是 em**：`em` 相对于**当前元素**（或继承的）`font-size`。例如 `.tree-node-icon { font-size: 0.9rem; min-width: 1.2em }`，`min-width` 实际 = 1.2 × 0.9rem。

**为什么混合 px 和 rem 会出问题**：

```css
/* ❌ 错误：容器用 px，文字用 rem，字体缩放时文字溢出 */
.tree-node-icon { width: 16px; font-size: 0.9rem; }
/* 字体放大 1.5x 时：文字 = 0.9×13.5×1.5 = 18.2px，但容器只有 16px → 溢出重叠 */

/* ✅ 正确：容器用 em，随文字等比缩放 */
.tree-node-icon { min-width: 1.2em; font-size: 0.9rem; }
/* 字体放大 1.5x 时：容器 = 1.2×18.2 = 21.8px，文字不溢出 */
```

**选择规则**：

| 场景 | 用什么 | 原因 |
|------|--------|------|
| 字体大小 | `rem` | 受根字体缩放控制，"用户调大字体 → 所有文字变大" |
| 图标容器宽度 | `em` | 相对自身 font-size，图标始终不被截断 |
| flex gap / padding | `em` | 随字体缩放保持比例 |
| 边框粗细 | `px` | 通常不需要缩放（1px 边框在各字号下都合适） |
| 固定布局（如侧边栏宽度） | `px` 或 `rem` | 取决于设计意图 |

**SVG 图标缩放**：SVG 有 `width`/`height` 属性（如 `width="14"`），在缩放布局中需要用 CSS 覆盖：
```css
.tree-node-icon svg { width: 1em; height: 1em; }
```
这样 SVG 图标随父元素的 `font-size` 等比缩放。

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

## 12. 开发命令速查

```bash
npm run tauri dev    # 启动开发环境（Vite + Tauri + Python 后端自动启动）
npm run tauri build  # 生产构建（打包 exe）
npm run dev          # 单独启动 Vite 开发服务器
npx vite build       # 单独构建前端
```

### 3.4 侧边打开流程

```
用户右键文件树中的文件 → "在侧边打开"
  → FileExplorer.openFileInSide(node)
    → App.showPage('editor')
    → Editor.openFileInSide(node.path, node.name)
      → 若 splitActive=false，调用 toggleSplit() 显示右侧面板
      → openFileInPanel(otherPanel, path, name)
        → Bridge.readTextFile(path)
        → 在右侧面板创建 CM6 实例或切换标签页
```

### 3.5 右键菜单流程

```
用户右键文件树文件节点
  → contextmenu 事件 → showFileContextMenu(x, y, node)
  → 根据文件类型动态生成菜单项
  → 用户点击菜单项 → 调用对应函数（openFile / openFileInSide / openInBrowser / openWithPreview）
```
---

> **最后更新**：2026-07-12
> **维护规则**：每次完成任务后补充新知识点到对应章节。
> **本次更新摘要**：文件树字体缩放时图标重叠修复（px → em）、可缩放布局中 CSS 单位选择（px/em/rem 对比表 + SVG 缩放）。
> **本次更新摘要**：修复未保存标记/复制失效、删除自动换行和分屏、编辑器字体加大+深色颜色优化、浅色模式命名"浅梦微光"、Markdown/HTML 预览功能、CodeMirror 链接权限弹窗。
